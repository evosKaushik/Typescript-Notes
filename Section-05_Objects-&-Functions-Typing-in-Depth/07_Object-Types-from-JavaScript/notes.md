# 📝 Object Types from JavaScript

object is come from JS object so it start form lower case

```ts 
const obj: object = {a: "qwe"}
```

* it only accept non-primitive object
* It not use in real world 
* we can use it rarely
