# 📝 Function Overloading in TypeScript

### In function overloading we accept same value but of different types then aur function logic is different for different types

Ex: for a string to give uppercase and for number it give square 

`We can do this by using typeof condition check`